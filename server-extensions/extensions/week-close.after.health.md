---
name: week-close.after.health
description: WP-470 Ф5 — секция «Здоровье (РП-470)» в WeekReport (недельное среднее + тренд первая/вторая половина недели, сон/пульс/плавание)
hook: after
protocol: week-close
valid_from: 2026-07-25
owner: user
---

# Week Close — секция здоровья (РП-470)

## Секция «Здоровье (РП-470)» в WeekReport (БЛОКИРУЮЩЕЕ при наличии базы)

**Когда выполнять:** шаг 9 (Extensions after) SKILL week-close — после шага 8 (запись итогов в WeekReport), до шага 11 (коммит).

**Дизайн:** по образцу `extensions/month-close.after.health.md` (решение пилота 18.07 для месяца), тот же принцип на недельном горизонте — три метрики (сон, пульс покоя, плавание), обязательный маркер покрытия «N из D дней», честная заглушка при отсутствии данных вместо тихого пропуска.

**Что сделать:**

```bash
# WEEK_START/WEEK_END — понедельник..воскресенье закрываемой недели (не текущей даты)
WEEK_START="YYYY-MM-DD"
WEEK_END="YYYY-MM-DD"
REPORT_FILE="$HOME/IWE/DS-my-strategy/current/WeekReport W{N} YYYY-MM-DD.md"

if grep -q "## Здоровье (РП-470)" "$REPORT_FILE"; then
  echo "week-close.after.health: секция уже присутствует — пропуск (idempotent)"
else
  bash "$HOME/IWE/extensions/week-close.summary-extra.sh" "$WEEK_START" "$WEEK_END" >> "$REPORT_FILE"
fi
```

**Инвариант:**
- Данных нет → скрипт сам пишет честную заглушку, секция всё равно присутствует.
- Вывод детерминированный (SQLite), LLM его не переписывает.
- Скрипт author-only (личные данные), в FMT-exocortex-template не поставляется.

**Проверка перед коммитом:** `grep -c "Здоровье (РП-470)" "$REPORT_FILE"` → ровно 1.

**Не встроено (решение отложено):** привязка к стоп-листу «перегрузка/выгорание» — на 25.07 такой проверки в SKILL week-close нет (grep по файлу ничего не находит), поэтому секция существует как самостоятельный информационный блок, не как вход в решающее правило. Если пилот решит завести критерий перегрузки — эта секция станет для него источником данных, доработка отдельным шагом.
